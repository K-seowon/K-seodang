# items 폴더

여기에 아이템 그림 파일(PNG 등)을 넣으세요.

파일 이름은 `index.html`의 `ITEMS` 자료에서 지정한 `src` 경로와
정확히 같아야 합니다. 예: `mhs_16:{ media:'img', src:'items/mhs_16.png', ... }`
→ `items/mhs_16.png` 파일을 이 폴더에 넣으면 됩니다.

파일이 아직 없으면 게임 안에서는 점선 테두리에 "그림 없음"이라고
쓰인 자리가 대신 나타납니다.
